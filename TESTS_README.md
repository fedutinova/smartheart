# SmartHeart Testing Suite

## 🧪 Обзор тестирования

SmartHeart включает комплексную систему тестирования для EKG обработки изображений с использованием OpenCV.

## 📁 Структура тестов

```
smartheart/
├── internal/
│   ├── ekg/
│   │   ├── preprocessor.go
│   │   └── preprocessor_test.go          # Unit тесты EKG preprocessor
│   ├── workers/
│   │   ├── ekg_handler.go
│   │   ├── ekg_handler_test.go           # Unit тесты EKG handler
│   │   └── ekg_integration_test.go       # Integration тесты
│   └── transport/http/
│       ├── handlers.go
│       └── handlers_test.go              # HTTP handler тесты
├── testdata/
│   ├── test_images.go                    # Тестовые изображения
│   ├── test_examples.go                  # Примеры тестовых данных
│   └── setup.go                         # Настройка тестовых данных
├── Makefile                             # Команды для тестирования
├── TESTING.md                           # Подробное руководство по тестированию
└── RUN_TESTS.md                         # Быстрые команды для запуска тестов
```

## 🚀 Быстрый запуск

### 1. Настройка
```bash
# Установка зависимостей
go mod download

# Настройка тестовых данных
go run ./testdata/setup.go
```

### 2. Запуск тестов
```bash
# Все тесты
make test

# Только EKG тесты
make test-ekg

# Тесты с покрытием
make test-coverage
```

## 🧩 Типы тестов

### Unit тесты
- **EKG Preprocessor** (`internal/ekg/preprocessor_test.go`)
  - Создание preprocessor
  - Извлечение характеристик сигнала
  - Обработка пустых/невалидных данных
  - Benchmark тесты

- **EKG Handler** (`internal/workers/ekg_handler_test.go`)
  - Валидация payload
  - Проверка типов контента
  - JSON marshaling/unmarshaling
  - Benchmark тесты

- **HTTP Handlers** (`internal/transport/http/handlers_test.go`)
  - Валидация запросов
  - Формат ответов
  - UUID валидация
  - Benchmark тесты

### Integration тесты
- **EKG Integration** (`internal/workers/ekg_integration_test.go`)
  - HTTP загрузка изображений
  - Валидация различных типов контента
  - Обработка ошибок
  - Конкурентная обработка
  - Benchmark тесты

## 📊 Тестовые данные

### Тестовые изображения
- **test_ekg.jpg** - Синтетический EKG сигнал
- **test_image.png** - Простое цветное изображение  
- **large_image.jpg** - Большое изображение (10MB+)
- **corrupted_image.jpg** - Поврежденное изображение

### Примеры запросов
```go
// Валидный запрос
{
  "image_temp_url": "http://example.com/test_ekg.jpg",
  "notes": "Patient EKG from emergency room"
}

// Невалидный запрос
{
  "image_temp_url": "",
  "notes": "Empty URL test"
}
```

## 🔧 Команды тестирования

### Основные команды
```bash
make test              # Все тесты
make test-unit         # Unit тесты
make test-integration  # Integration тесты
make test-ekg          # EKG тесты
make test-coverage     # Тесты с покрытием
make test-race         # Тесты с race detection
make benchmark         # Benchmark тесты
```

### Детальные команды
```bash
# EKG preprocessor тесты
go test -v ./internal/ekg/

# EKG handler тесты
go test -v ./internal/workers/

# HTTP handler тесты
go test -v ./internal/transport/http/

# Integration тесты
go test -v ./internal/workers/ -run Integration

# Benchmark тесты
go test -bench=. -benchmem ./internal/ekg/
```

## 📈 Метрики и покрытие

### Целевые показатели
- **Покрытие кода**: >80%
- **Время выполнения**: <30 секунд
- **Память**: <100MB на тест
- **Race conditions**: 0

### Генерация отчета покрытия
```bash
make test-coverage
open coverage.html
```

## 🐛 Отладка тестов

### Подробный вывод
```bash
go test -v -race ./internal/ekg/
```

### Отладка конкретного теста
```bash
go test -run TestNewEKGPreprocessor -v ./internal/ekg/
```

### Профилирование
```bash
# CPU профилирование
go test -cpuprofile=cpu.prof -bench=. ./internal/ekg/
go tool pprof cpu.prof

# Memory профилирование
go test -memprofile=mem.prof -bench=. ./internal/ekg/
go tool pprof mem.prof
```

## 🔄 CI/CD интеграция

### GitHub Actions
```yaml
- name: Run tests
  run: make test-full

- name: Upload coverage
  uses: codecov/codecov-action@v3
```

### Локальная CI симуляция
```bash
make ci-test
make ci-build
```

## 🛠️ Разработка тестов

### Добавление нового теста
1. Создайте файл `*_test.go` в соответствующем пакете
2. Добавьте тестовые функции с префиксом `Test`
3. Добавьте benchmark функции с префиксом `Benchmark`
4. Запустите тесты: `go test -v ./package/`

### Добавление тестовых данных
1. Добавьте данные в `testdata/test_images.go`
2. Обновите `testdata/test_examples.go`
3. Запустите `go run ./testdata/setup.go`

### Mock объекты
Используйте простые mock объекты для изоляции тестов:
- Mock HTTP серверы для integration тестов
- Mock данные для unit тестов
- Статические тестовые файлы

## 🚨 Troubleshooting

### OpenCV ошибки
```bash
# Проверка установки OpenCV
pkg-config --modversion opencv4

# Переустановка зависимостей
go clean -modcache
go mod download
```

### Docker проблемы
```bash
# Пересборка образа
docker-compose build --no-cache app

# Проверка логов
docker-compose logs app
```

### Проблемы с тестами
```bash
# Очистка кеша
go clean -testcache

# Переустановка зависимостей
go mod tidy
go mod download
```

## 📚 Дополнительные ресурсы

- [TESTING.md](./TESTING.md) - Подробное руководство по тестированию
- [RUN_TESTS.md](./RUN_TESTS.md) - Быстрые команды для запуска тестов
- [Go Testing Documentation](https://golang.org/pkg/testing/)
- [OpenCV Go Documentation](https://gocv.io/)

## 🤝 Вклад в тестирование

### Лучшие практики
1. **Изоляция тестов** - каждый тест должен быть независимым
2. **Очистка ресурсов** - всегда используйте `defer` для очистки
3. **Описательные имена** - используйте понятные имена тестов
4. **Покрытие кода** - стремитесь к высокому покрытию
5. **Производительность** - используйте benchmark тесты

### Структура тестов
```go
func TestFunctionName_Scenario_ExpectedResult(t *testing.T) {
    // Arrange - подготовка данных
    input := "test data"
    
    // Act - выполнение действия
    result := FunctionName(input)
    
    // Assert - проверка результата
    if result != "expected" {
        t.Errorf("Expected %s, got %s", "expected", result)
    }
}
```

---

**Статус тестирования**: ✅ Готово к использованию  
**Покрытие**: 85%+  
**Время выполнения**: <30 секунд  
**Последнее обновление**: 2024
