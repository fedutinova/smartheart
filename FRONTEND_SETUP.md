# Настройка фронтенда SmartHeart

## 🚀 Как запустить

### Шаг 1: Установка зависимостей

```bash
cd frontend
npm install
```

### Шаг 2: Создайте .env файл

```bash
cp env.example .env
```

Или создайте вручную:
```bash
echo "VITE_API_URL=http://localhost:8080" > .env
```

### Шаг 3: Запустите dev сервер

```bash
npm run dev
```

Фронтенд будет доступен на **http://localhost:3000**

### Шаг 4: Запустите бэкенд

В другом терминале:
```bash
cd ..
docker-compose up -d postgres redis localstack
CGO_ENABLED=1 go run cmd/main.go
```

Бэкенд будет на **http://localhost:8080**

## 🎯 Основные возможности

### ✅ Реализовано:

1. **Авторизация**
   - Регистрация новых пользователей
   - Вход по email/password
   - JWT токены с автоматическим refresh
   - Logout функционал

2. **ЭКГ Анализ**
   - Форма загрузки по URL
   - Предпросмотр изображения
   - Отправка на анализ
   - Polling статуса обработки

3. **Dashboard**
   - Последние анализы
   - Быстрые ссылки
   - Статистика

4. **История**
   - Таблица всех запросов
   - Фильтрация по статусу
   - Ссылки на детали

5. **Результаты**
   - Детальная информация о сигнале
   - Характеристики (длина, ширина, амплитуда и т.д.)
   - Шаги обработки
   - Bounding box информация

6. **UI/UX**
   - Responsive дизайн
   - Tailwind CSS стилизация
   - Индикаторы статусов
   - Красивые карточки

## 🛠 Технологии

- **React 18** - UI framework
- **TypeScript** - Статическая типизация
- **Vite** - Быстрый dev server и bundler
- **React Router v6** - Клиентская маршрутизация
- **React Query** - Server state management
- **Zustand** - Global state (auth)
- **Axios** - HTTP клиент
- **Tailwind CSS** - Utility-first CSS
- **date-fns** - Форматирование дат

## 📝 API Endpoints (используемые)

- `POST /v1/auth/register` - Регистрация
- `POST /v1/auth/login` - Вход
- `POST /v1/auth/refresh` - Обновление токена
- `POST /v1/auth/logout` - Выход
- `POST /v1/ekg/analyze` - Анализ ЭКГ
- `GET /v1/jobs/{id}` - Статус задачи
- `GET /v1/requests` - Список запросов пользователя
- `GET /v1/requests/{id}` - Детали запроса

## 🔮 Возможные улучшения

1. **Визуализация ЭКГ**
   - График сигнала на canvas/SVG
   - Интерактивные элементы
   - Zoom/pan функциональность

2. **Загрузка файлов**
   - Drag-and-drop
   - Прогресс бар
   - Поддержка множественных файлов

3. **Экспорт результатов**
   - PDF отчеты
   - CSV данные
   - Печать результатов

4. **Улучшения UX**
   - Dark mode
   - Notifications в реальном времени
   - WebSocket обновления
   - Skeleton loading
   - Toast уведомления

5. **Админ панель**
   - Управление пользователями
   - Мониторинг статистики
   - Логи системы

6. **Дополнительные фичи**
   - Фильтры и поиск в истории
   - Пагинация
   - i18n (мультиязычность)
   - PWA поддержка
   - Offline режим

## 📚 Документация

- `frontend/README.md` - Полная документация
- `frontend/QUICKSTART.md` - Быстрый старт
- `README.md` - Основной README с разделом о фронтенде

## 🐛 Troubleshooting

**Ошибка: Cannot find module**
```bash
rm -rf node_modules package-lock.json
npm install
```

**Ошибка: Tailwind не работает**
```bash
npm install -D tailwindcss postcss autoprefixer
```

**CORS ошибки**
- CORS middleware уже настроен в `internal/server/router.go`
- Перезапустите бэкенд после изменений: `CGO_ENABLED=1 go run cmd/main.go`
- Убедитесь, что ответ на OPTIONS запросы возвращает 200 OK

**Не загружаются данные**
- Проверьте, что бэкенд запущен на порту 8080
- Проверьте .env файл
- Откройте DevTools → Network для отладки

