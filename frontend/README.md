# SmartHeart Frontend

Веб-интерфейс для системы анализа EKG SmartHeart.

## Технологии

- **React 18** - UI библиотека
- **TypeScript** - Типизация
- **Vite** - Сборщик и dev сервер
- **React Router** - Маршрутизация
- **React Query** - Управление серверным состоянием
- **Zustand** - Глобальное состояние
- **Axios** - HTTP клиент
- **Tailwind CSS** - Стилизация

## Установка

```bash
# Установить зависимости
npm install

# Запустить dev сервер
npm run dev

# Собрать для production
npm run build

# Просмотреть production сборку
npm run preview
```

## Конфигурация

Создайте файл `.env` в корне `frontend/`:

```bash
VITE_API_URL=http://localhost:8080
```

## Структура проекта

```
frontend/
├── src/
│   ├── components/      # Переиспользуемые компоненты
│   │   ├── Layout.tsx
│   │   └── ProtectedRoute.tsx
│   ├── pages/          # Страницы приложения
│   │   ├── Login.tsx
│   │   ├── Register.tsx
│   │   ├── Dashboard.tsx
│   │   ├── Analyze.tsx
│   │   ├── History.tsx
│   │   └── Results.tsx
│   ├── services/       # API клиенты
│   │   └── api.ts
│   ├── store/          # Zustand хранилища
│   │   └── auth.ts
│   ├── types/          # TypeScript типы
│   │   └── index.ts
│   ├── utils/          # Утилиты
│   │   ├── format.ts
│   │   └── storage.ts
│   ├── config.ts       # Конфигурация
│   ├── App.tsx         # Корневой компонент
│   ├── main.tsx        # Точка входа
│   └── index.css       # Глобальные стили
├── index.html
├── package.json
├── vite.config.ts
├── tailwind.config.js
└── tsconfig.json
```

## Функционал

### 🔐 Аутентификация
- Регистрация новых пользователей
- Вход по email/паролю
- JWT токены с refresh механизмом
- Автоматический logout при истечении токена

### 📊 Анализ EKG
- Загрузка изображения по URL
- Предпросмотр изображения
- Отправка на анализ
- Отслеживание статуса обработки

### 📈 Результаты
- Детальная информация о сигнале
- Визуализация характеристик
- История всех запросов
- Фильтрация по статусу

### 🎨 UI/UX
- Адаптивный дизайн
- Красивые карточки и таблицы
- Цветовая индикация статусов
- Плавные переходы

## API Endpoints

Фронтенд использует следующие endpoints бэкенда:

- `POST /v1/auth/register` - Регистрация
- `POST /v1/auth/login` - Вход
- `POST /v1/auth/refresh` - Обновление токена
- `POST /v1/auth/logout` - Выход
- `POST /v1/ekg/analyze` - Анализ EKG
- `GET /v1/jobs/{id}` - Статус задачи
- `GET /v1/requests` - Список запросов
- `GET /v1/requests/{id}` - Детали запроса

## Разработка

```bash
# Запуск с hot-reload
npm run dev

# Проверка типов
npm run lint
```

## Deployment

```bash
# Сборка для production
npm run build

# Выходные файлы в dist/
```

## Улучшения

Возможные улучшения для будущей версии:

- [ ] Загрузка файлов drag-and-drop
- [ ] График сигнала EKG на canvas
- [ ] Экспорт результатов в PDF
- [ ] Уведомления в реальном времени
- [ ] Тема dark/light
- [ ] Управление пользователями (для админов)
- [ ] Фильтры и поиск в истории
- [ ] Пагинация для больших списков
- [ ] Мультиязычность (i18n)
- [ ] PWA поддержка

## Лицензия

MIT

